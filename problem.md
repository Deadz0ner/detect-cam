Assignment: “Basic AI Deployment Pipeline”
Problem Statement
Build a simple system that:
Takes a video (or webcam stream)
Detects people
Draws bounding boxes
If a person enters a defined region (ROI), print:
"ALERT: Person in restricted area"

Requirements
Use:
Python
OpenCV
Any pretrained model (YOLO / MobileNet / etc.)
Must include:
Frame processing loop
Basic ROI logic (hardcoded is fine)
Clean, readable code

Submission
GitHub repo
README with:
Setup steps
Approach
Limitations
